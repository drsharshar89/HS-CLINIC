# Sanity CMS Configuration
# Public (safe for browser — prefixed with VITE_)
VITE_SANITY_PROJECT_ID=your_project_id_here
VITE_SANITY_DATASET=production

# Secret (server-side only — NO VITE_ prefix, never shipped to browser)
# Get this from: https://www.sanity.io/manage → your project → API → Tokens
SANITY_API_TOKEN=your_secret_token_here

# ── Dataset Switching ──
# To use staging dataset instead of production:
#   Frontend: set VITE_SANITY_DATASET=staging
#   Studio:   set SANITY_STUDIO_DATASET=staging (before running `sanity dev`)
# Available datasets: production, staging
